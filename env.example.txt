# ============================================================
# AchoQ — Variáveis de Ambiente para AWS Amplify
# Configure estas variáveis em:
#   AWS Amplify Console → App Settings → Environment Variables
# ============================================================

# --- Banco de Dados (MySQL / PlanetScale / RDS) ---
DATABASE_URL=mysql://user:password@host:3306/achoq

# --- Segurança ---
JWT_SECRET=troque-por-uma-string-aleatoria-longa-e-segura
# Senha do painel /admin
ADMIN_PASSWORD=troque-por-uma-senha-forte
# Secret dos endpoints /api/scheduled/* (resolução de enquetes e fechamento
# da liga). Aceito como bearer JWT ou header x-scheduled-secret.
SCHEDULED_SECRET=troque-por-outra-string-aleatoria

# --- Manus OAuth (autenticação) ---
VITE_APP_ID=
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://manus.im
OWNER_OPEN_ID=
OWNER_NAME=

# --- Manus Forge API (LLM, Storage, Notificações) ---
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=

# --- App ---
NODE_ENV=production
VITE_APP_TITLE=AchoQ
